# Streaming producer/consumer for Kafka ingestion
FROM python:3.13-slim

RUN pip install "uv>=0.9,<1"

WORKDIR /app

COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-dev --extra streaming

COPY src/ ./src/
COPY config.toml ./

ENV PATH="/app/.venv/bin:$PATH"
ENV PYTHONPATH="/app"
